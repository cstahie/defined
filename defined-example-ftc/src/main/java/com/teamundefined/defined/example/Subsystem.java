package com.teamundefined.defined.example;

import com.teamundefined.defined.Slot;

/** The example robot's subsystem slots (your team declares its own like this). */
public enum Subsystem implements Slot {
    DRIVE, INTAKE, FLYWHEEL, INDEXER, TURRET
}
